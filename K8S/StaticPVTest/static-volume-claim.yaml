apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: static-persistence-volume-claim
  namespace: sftp
spec:
  accessModes:
    - ReadWriteMany  
  resources:
    requests:
      storage: 5Gi
  storageClassName: sc-azurefile-static