---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: static-persistence-volume-deployment
  namespace: sftp
  labels:
    environment: dev
    app: sftp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sftp
  template:
    metadata:
      labels:
        environment: dev
        app: sftp
    spec:
      containers:
      - image: goepjbossacr.azurecr.io/sftp_baseimage
        imagePullPolicy: Always
        name: sftp-container
        env:
        - name: SFTP_USERS
          value: 'foo:123:1000,bar:abc:1001'
        volumeMounts:
        - name: user1
          mountPath: /home/user1/in
        volumeMounts:
        - name: user2
          mountPath: /home/user2/in

      volumes:
      - name: user1
        persistentVolumeClaim:
          claimName: share-obama
      - name: user2
        persistentVolumeClaim:
          claimName: share-obama

