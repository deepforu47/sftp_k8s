---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sftp-pvc
  namespace: sftp
  labels:
    storage: sftp
spec:
  accessModes:
    - ReadWriteMany
  volumeMode: Filesystem
  storageClassName: sftpazurefile-sc
  resources:
    requests:
      storage: 5Gi